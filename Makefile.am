## Process this file with automake to produce Makefile.in

SUBDIRS = po m4 intl nih nih-dbus nih-dbus-tool dbus src conf man po

EXTRA_DIST = config.rpath ChangeLog.nih

ACLOCAL_AMFLAGS = --install -I m4
