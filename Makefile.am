## Process this file with automake to produce Makefile.in

SUBDIRS = intl dbus util src conf debian/initramfs debian/initramfs/upstart-jobs/ man po apport

EXTRA_DIST = config.rpath

ACLOCAL_AMFLAGS = --install -I m4
